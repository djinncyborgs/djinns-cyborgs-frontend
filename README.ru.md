# DjinTech

[English](./README.md) | **Русский**

Современное production-ready приложение на Next.js с TypeScript, построенное по методологии Feature-Sliced Design (FSD).

## 🤝 Участие в разработке

Пожалуйста, прочитайте [CONTRIBUTING.ru.md](./CONTRIBUTING.ru.md) для получения подробной информации о нашем кодексе поведения и соглашениях о коммитах.

## 🛠️ Стек технологий

### Ядро

- **[Next.js 16.1.4](https://nextjs.org)** - React фреймворк для production
- **[React 19.2.3](https://react.dev)** - Библиотека для UI
- **[TypeScript 5](https://www.typescriptlang.org)** - Типизация

### Управление состоянием и данными

- **[Zustand 5.0](https://zustand-demo.pmnd.rs)** - Легковесное управление состоянием
- **[TanStack Query 5.90](https://tanstack.com/query)** - Управление серверным состоянием
- **[Axios 1.13](https://axios-http.com)** - HTTP клиент

### Формы и валидация

- **[React Hook Form 7.71](https://react-hook-form.com)** - Производительные формы
- **[Zod 4.3](https://zod.dev)** - Схемы валидации с TypeScript
- **[@hookform/resolvers 5.2](https://github.com/react-hook-form/resolvers)** - Резолверы для валидации форм

### Интернационализация

- **[i18next 25.8](https://www.i18next.com)** - i18n фреймворк
- **[react-i18next 16.5](https://react.i18next.com)** - React интеграция
- **[i18next-browser-languagedetector 8.2](https://github.com/i18next/i18next-browser-languageDetector)** - Определение языка

### Стилизация

- **[Sass 1.97](https://sass-lang.com)** - CSS препроцессор
- **[clsx 2.1](https://github.com/lukeed/clsx)** - Утилита для работы с className

### Качество кода

- **[ESLint 9](https://eslint.org)** - Линтинг JavaScript/TypeScript
- **[Prettier 3.8](https://prettier.io)** - Форматирование кода
- **[Stylelint 17](https://stylelint.io)** - Линтинг CSS/SCSS
- **[TypeScript ESLint 8.53](https://typescript-eslint.io)** - Правила линтинга для TypeScript

## 🚀 Быстрый старт

### Требования

- Node.js 20+
- pnpm (рекомендуется) или npm/yarn

### Установка

```bash
# Установка зависимостей
pnpm install

# Запуск dev-сервера
pnpm dev
```

Откройте [http://localhost:3000](http://localhost:3000) для просмотра приложения.

### Доступные команды

```bash
# Разработка
pnpm dev                    # Запуск dev-сервера

# Сборка
pnpm build                  # Сборка для разработки
pnpm build:production       # Псевдоним для сборки в продакшн
pnpm start                  # Запуск production сервера

# Качество кода
pnpm lint                  # Запуск ESLint
pnpm lint:fix              # Исправить ошибки ESLint
pnpm format                # Проверка форматирования
pnpm format:fix            # Исправить форматирование
pnpm stylelint             # Линтинг стилей
pnpm stylelint:fix         # Исправить стили
```

## 🏗️ Структура проекта

Проект следует методологии **[Feature-Sliced Design (FSD)](https://feature-sliced.design)** — архитектурной методологии для фронтенд проектов.

### Иерархия слоёв

Нижележащие слои могут использоваться только слоями, стоящими выше по иерархии:

### `shared 🡒 entities 🡒 features 🡒 widgets 🡒 views 🡒 app`

**Правило:** Каждый слой может импортировать только из слоёв ниже. Кросс-импорты внутри одного слоя запрещены.

```text
├── public/                       # статические файлы (иконки, изображения и т.д.)
│   ├── favicon-dark.svg
│   ├── favicon-light.svg
│   └── ...
├── src/
│   ├── app/                      # Next.js App Router (роутинг и лэйауты)
│   │   ├── (auth)/
│   │   ├── dashboard/
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── views/                    # композиции представлений (связывают widgets, features, entities)
│   │   ├── home/
│   │   ├── login/
│   │   ├── userProfile/
│   │   └── ...
│   ├── widgets/                  # составные UI блоки (header, footer, sidebar и т.д.)
│   │   ├── header/
│   │   ├── footer/
│   │   └── ...
│   ├── features/                 # пользовательские сценарии (login, logout, toggleTheme и т.д.)
│   │   ├── auth/
│   │   ├── theme/
│   │   └── ...
│   ├── entities/                 # бизнес-сущности (user, product, session и т.д.)
│   │   ├── user/
│   │   ├── product/
│   │   └── ...
│   └── shared/                   # переиспользуемая инфраструктура (без бизнес-логики)
│       ├── api/
│       ├── assets/
│       ├── components/
│       ├── configs/
│       ├── fonts/
│       ├── hooks/
│       ├── icons/
│       ├── styles/
│       ├── translations/
│       ├── types/
│       └── utils/
├── .editorconfig
├── eslint.config.mjs
├── next.config.ts
├── package.json
└── ...
```

### Описание слоёв FSD

#### **App слой** (`src/app/`)

Next.js App Router - только роутинг и лэйауты:

- Сегменты маршрутов (папки с `page.tsx`)
- Лэйауты (`layout.tsx`)
- Группы маршрутов с `(groupName)` для организации
- Состояния загрузки и ошибок
- **Без бизнес-логики** - только импорты из других слоёв

Пример:

```tsx
// app/dashboard/page.tsx
import {DashboardView} from '@/views/dashboard';

export default function Dashboard() {
  return <DashboardView />;
}
```

#### **Views слой** (`src/views/`)

Композиции страниц - связывает widgets, features и entities:

- Одна папка на страницу (например, `homePage/`, `loginPage/`)
- Содержит только композицию UI
- Управляет состоянием на уровне страницы при необходимости
- Без логики роутинга (обрабатывается в `app/`)

**Сегменты**: `ui/`, `model/` (опционально)

Пример структуры:

```text
views/
└── homePage/
    ├── ui/
    │   ├── HomePage.tsx
    │   └── HomePage.module.scss
    ├── model/                    # Опционально: состояние страницы
    │   └── useHomeData.ts
    └── index.ts                  # Public API: export { default } from './ui/HomePage'
```

#### **Widgets слой** (`src/widgets/`)

Крупные составные блоки - самодостаточные UI секции:

- Header, Footer, Sidebar, Navigation
- Сложные формы и модальные окна
- Виджеты и карточки дашборда
- Составлены из features, entities и shared компонентов

**Сегменты**: `ui/`, `model/`, `api/`, `lib/`, `config/`

Пример структуры:

```text
widgets/
└── header/
    ├── ui/
    │   ├── Header.tsx
    │   └── Header.module.scss
    ├── model/                    # Опционально: состояние виджета
    │   └── useHeaderState.ts
    └── index.ts                  # Public API: export { Header } from './ui/Header'
```

#### **Features слой** (`src/features/`)

Пользовательские взаимодействия - законченные пользовательские сценарии:

- Аутентификация (логин, регистрация, выход)
- Действия с продуктами (добавить в корзину, в избранное)
- Управление контентом (создать пост, редактировать комментарий)
- Каждая фича изолирована и переиспользуема

**Сегменты**: `ui/`, `model/`, `api/`, `lib/`, `config/`

Пример структуры:

```text
features/
└── auth/
    └── login/
        ├── ui/
        │   ├── LoginForm.tsx
        │   └── LoginForm.module.scss
        ├── model/
        │   └── loginStore.ts
        ├── api/
        │   └── loginApi.ts
        └── index.ts              # Public API: export { LoginForm } from './ui/LoginForm'
```

#### **Entities слой** (`src/entities/`)

Бизнес-сущности - доменные модели:

- User, Product, Order, Cart и т.д.
- Модели данных, API методы, хранилища
- Переиспользуемый UI для отображения сущностей
- Без логики конкретных фич

**Сегменты**: `ui/`, `model/`, `api/`, `lib/`, `config/`

Пример структуры:

```text
entities/
└── user/
    ├── ui/
    │   ├── UserCard.tsx
    │   └── UserAvatar.tsx
    ├── model/
    │   ├── userStore.ts
    │   └── user.types.ts
    ├── api/
    │   └── userApi.ts
    └── index.ts                  # Public API: export * from './ui'; export * from './model'
```

#### **Shared слой** (`src/shared/`)

Переиспользуемая инфраструктура - без бизнес-логики:

- **components/**: UI kit (Button, Input, Modal)
- **hooks/**: Кастомные React хуки (useDebounce, useMediaQuery)
- **utils/**: Вспомогательные функции (formatDate, cn, validators)
- **styles/**: Глобальные стили, переменные, миксины
- **configs/**: Конфигурации приложения
- **types/**: Общие TypeScript типы
- **translations/**: Файлы переводов i18n
- **icons/**: Компоненты иконок
- **assets/**: Статические файлы (изображения, шрифты)

**Примечание**: В `shared/` каждая подпапка (components, hooks, utils и т.д.) может иметь `index.ts` для удобного экспорта.

Пример структуры:

```text
shared/
├── components/
│   ├── Button/
│   │   ├── Button.tsx
│   │   ├── Button.module.scss
│   │   └── index.ts            # export { Button } from './Button'
│   └── index.ts                # export { Button } from './Button'
└── utils/
    ├── cn.ts
    ├── formatDate.ts
    └── index.ts                # export * from './cn'; export * from './formatDate'
```

### 📦 Паттерн Public API (index.ts)

**Каждый слайс должен иметь `index.ts`**, который экспортирует только то, что нужно другим слоям:

```typescript
// features/auth/login/index.ts
export {LoginForm} from './ui/LoginForm';
export {useLogin} from './model/useLogin';
// Не экспортируем детали внутренней реализации
```

**Преимущества:**

- ✅ Чёткий публичный API для каждого слайса
- ✅ Простой рефакторинг внутренней структуры
- ✅ Предотвращение импорта внутренней реализации
- ✅ Чистые импорты: `import { LoginForm } from '@/features/auth/login'`

**Где использовать index.ts:**

- ✅ В корне каждого слайса (например, `features/auth/login/index.ts`)
- ✅ `shared/components/index.ts`, `shared/utils/index.ts` и т.д.
- ❌ Не нужен внутри сегментов (ui/, model/, api/)

### Правила импортов

Следуйте этим правилам импортов для поддержания независимости слоёв:

```text
app → views → widgets → features → entities → shared
  ↓      ↓        ↓         ↓          ↓         ↓
Можно импортировать только из слоёв ниже
```

**Примеры:**

- ✅ `views/homePage` может импортировать из `widgets`, `features`, `entities`, `shared`
- ✅ `features/login` может импортировать из `entities/user`, `shared/components`
- ❌ `entities/user` **не может** импортировать из `features/login`
- ❌ `shared/components` **не может** импортировать из `entities/user`

## 🚢 Развёртывание

Проект включает конфигурации Docker и CI/CD:

- `Dockerfile` - Конфигурация контейнера
- `Jenkinsfile` - CI/CD пайплайн
- `deployment.yaml` - Kubernetes развёртывание
- `nginx.conf` - Конфигурация Nginx

## 📚 Узнать больше

- [Документация Next.js](https://nextjs.org/docs)
- [Feature-Sliced Design](https://feature-sliced.design)
- [Документация React](https://react.dev)
- [TypeScript Handbook](https://www.typescriptlang.org/docs)

# Участие в разработке

[English](./CONTRIBUTING.md) | **Русский**

Этот документ описывает правила участия в разработке проекта, включая требования к сообщениям коммитов, стилю кода и соглашениям по именованию файлов.

## Git‑флоу проекта

### Ветки

- "development" — основная рабочая ветка (по умолчанию)
- "main" — ветка продакшн-кода

### Рабочий процесс разработчика

1. Клонируйте репозиторий и переключитесь на "development".
2. Создайте feature-ветку от "development" (например, "feature/DT-123").
3. Разрабатывайте, тестируйте локально, собирайте проект и пушьте изменения.
4. Создайте Pull Request (PR) из своей ветки в "development".
5. Переведите задачу в Jira в колонку **Review**.
6. После ревью и approve PR сливается в "development".
   > Держите ветку синхронизированной с "development", чтобы избежать конфликтов.

### Релиз / Тимлид

1. Создаётся PR из "development" → "main".
2. CI/CD выполняет:
   - тесты
   - сборку проекта
   - создание образа
   - push в DockerHub
   - деплой в Kubernetes
3. Задачи в Jira, которые были смерджены в "development", переводятся в **Testing** и тестировщикам отправляется уведомление.
4. Тестировщик:
   - если нет багов — задача переводится в **Ready for Release**
   - если есть баги — в **Stabilization**
5. Для багов разработчик создаёт новую ветку от "development" с постфиксом (например, "DT-123-2") и повторяет процесс разработки.
6. Когда тестирование прошло успешно, код мержится в "main", а задача в Jira переводится в **Done**.

### Лучшие практики

- Merge в "development" только после согласования
- "development" всегда должна быть рабочей и актуальной
- Частые маленькие релизы
- Разработчик должен подтягивать "development" в свою ветку ежедневно
- Перед merge убедитесь, что все тесты проходят успешно

## 📋 Сообщения коммитов

Сообщения коммитов должны следовать спецификации [Conventional Commits](https://conventionalcommits.org):

```markdown
<type>[optional scope]: <description>
```

### Допустимые `<type>`

- `chore`: любые изменения для поддержки репозитория
- `feat`: изменение кода, добавляющее новую функциональность
- `fix`: исправление ошибки
- `perf`: изменение кода, улучшающее производительность
- `refactor`: изменение кода, которое не является ни добавлением функции, ни исправлением ошибки, ни улучшением производительности
- `docs`: изменения только в документации
- `ci`: изменение CI конфигураций и скриптов
- `style`: косметическое изменение кода
- `test`: изменение, которое только добавляет или исправляет тесты
- `revert`: изменение, которое отменяет предыдущие коммиты

## Допустимые `<scope>`

Имя директории пакета. Например: `/packages/effects` имеет scope `effects`.

### Правила `<description>`

- должно быть написано на английском языке
- должно быть в повелительном наклонении (например, `change` вместо `changed` или `changes`)
- не должно начинаться с заглавной буквы
- не должно иметь точку (`.`) в конце

### Примеры сообщений коммитов

```bash
docs: fix typo in README
fix(auth): add validation for login form
feat(user): add user avatar component
refactor(shared): simplify cn utility function
chore(deps): update next to 16.1.4
```

## 📁 Соглашения об именовании файлов

| Категория                 | Формат                                    | Примеры                                            |
| ------------------------- | ----------------------------------------- | -------------------------------------------------- |
| **Компоненты**            | PascalCase                                | `UserCard.tsx`, `Button.tsx`, `LoginForm.tsx`      |
| **Хуки**                  | camelCase с префиксом `use`               | `useAuth.ts`, `useDebounce.ts`, `useMediaQuery.ts` |
| **Утилиты**               | camelCase                                 | `formatDate.ts`, `validateEmail.ts`, `cn.ts`       |
| **Хранилища** (Zustand)   | camelCase с суффиксом `Store`             | `authStore.ts`, `userStore.ts`                     |
| **API файлы**             | camelCase с суффиксом `Api`               | `authApi.ts`, `userApi.ts`                         |
| **Типы / Интерфейсы**     | PascalCase для типов, dot.case для файлов | `User.ts`, `ButtonProps.ts`, файл: `auth.types.ts` |
| **Константы**             | UPPER_SNAKE_CASE                          | `MAX_LENGTH = 100`, `API_URL`                      |
| **Стили (SCSS)**          | Совпадает с именем компонента             | `Button.module.scss`, `Header.module.scss`         |
| **Конфиг файлы**          | camelCase с суффиксом `.config`           | `app.config.ts`, `api.config.ts`                   |
| **Представления** (Views) | Папка: camelCase,                         | Папки: `home/`, `userProfile/`,                    |
|                           | Компонент: PascalCase + суффикс View      | Компонент: `HomeView`, `UserProfileView`           |

---

Спасибо за ваш вклад! 🎉
